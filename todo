
一个UDP+一个TCP客户端监听（保持原来服务架构）用于业务逻辑的通讯

三个UDP定位接收线程,每个线程监听一个端口，收到数据后存入同一个缓冲接收队列。

N个算法处理线程，挂在接收队列上读取数据，收到不到数据则挂起。

 
每个anchorGroup节点都有一个用户链表，用于将当前登入到这个房间的用户节点登记串接起来，当定位计算出某一标签的定位数据时，就会根据该链表发送定位数据给对用房间的用户。


每个tagNode都有一个轨迹点缓冲队列（默认容量30个点/可设置），数据缓冲完(凑成一组数据)后再发送，并且发送给客户端的轨迹点要先转换为整数。

只支持广播和单播，不支持组播。(用户要么选择浏览整个房间的轨迹点，要么只看一个轨迹点）


